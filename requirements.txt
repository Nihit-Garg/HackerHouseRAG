# Core ML / NLP
datasets>=2.20.0
duckdb>=1.0.0              # Native parquet reader via hf:// — handles nested schemas
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
tiktoken>=0.7.0
nltk>=3.9.0

# Vector store / retrieval (numpy pinned for FAISS compatibility)
numpy>=1.24.0,<2.0.0

# LLM
ollama>=0.3.0

# Web framework
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9     # required for FastAPI file uploads

# Orchestration
langgraph>=0.2.0

# HTTP client (STT + Ollama REST fallback)
requests>=2.32.0

# Config
python-dotenv>=1.0.0

# Typing
typing-extensions>=4.12.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.23.0
